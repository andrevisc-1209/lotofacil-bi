name: Atualizar Dupla Sena

on:
  schedule:
    - cron: '0 23 * * 2,6'   # Terça e Sábado às 23h UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  atualizar:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Atualizar banco
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python duplasena_atualizar.py

      - name: Gerar dashboard
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
        run: python duplasena_bi.py --source supabase

      - name: Commit e push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add duplasena.html
          git diff --staged --quiet || git commit -m "chore: atualiza Dupla Sena $(date '+%d/%m/%Y')"
          git push
